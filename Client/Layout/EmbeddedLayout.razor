@inherits BaseComponent
<RadzenComponents />
@Body
<script>
    document.querySelector('a').addEventListener('click', function(event) {
        event.preventDefault();  // Prevents the default iframe behavior
        window.parent.location.href = this.href;  // Opens the link in the parent
    });
</script>
